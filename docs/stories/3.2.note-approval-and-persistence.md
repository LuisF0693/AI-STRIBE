# Story 3.2: Note Approval & Persistence

## Status
InReview

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["code-review", "rls-validation", "api-contract-test", "migration-safety"]
```

## Story
**As a** médico,
**I want** aprovar a nota SOAP revisada com um toque e ter a versão final salva com segurança,
**so that** a nota clínica seja preservada com rastreabilidade completa (quem aprovou, quando) e fique disponível para exportação PDF (Story 3.3).

## Acceptance Criteria
1. Endpoint `PATCH /api/v1/notas/:id` salva edições de rascunho do médico — não altera `status`, apenas persiste o `soap_json` editado e os `cids_sugeridos` selecionados
2. Endpoint `POST /api/v1/notas/:id/aprovar` finaliza a nota: atualiza `status` de `'draft'` para `'approved'`, registra `approved_by` (medico_id) e `approved_at` (timestamp UTC)
3. A aprovação cria automaticamente um snapshot imutável em `notas_versoes` com `soap_json` final e `editado_por`
4. Idempotência: chamar `POST /api/v1/notas/:id/aprovar` em nota já aprovada retorna 200 com o estado atual — sem erro, sem duplicação
5. RLS Supabase: médico só pode editar ou aprovar notas associadas às suas próprias consultas — `supabaseAdmin` nunca usado no client mobile
6. Transição de status respeita a state machine (`updateNotaStatus()`) — direto UPDATE no banco sem validação é bloqueado pelo padrão da codebase
7. Após aprovação bem-sucedida, Supabase Realtime notifica o mobile com evento `nota:approved` (canal `consulta:{id}`)
8. Mobile: ao receber confirmação de aprovação, Zustand store é atualizado (`nota.status = 'approved'`) e usuário é redirecionado para a tela de exportação PDF
9. Histórico de versões: todas as edições intermediárias (PATCH) são registradas em `notas_versoes` para auditoria LGPD
10. Retorno de erro padronizado (`ApiError`) para: nota não encontrada (404), nota já aprovada sem mudanças (200 idempotente), acesso negado (403)

## Escopo

**IN (esta story):**
- Endpoint PATCH para salvar rascunho editado
- Endpoint POST para aprovação final da nota
- State machine `updateNotaStatus()` com validação de transição
- Histórico de versões em `notas_versoes`
- Notificação Realtime `nota:approved`
- Migration 007 (approved_by, approved_at, pdf_status)

**OUT (fora desta story):**
- Geração de PDF → Story 3.3
- Interface de revisão da nota → Story 3.1
- Reprovação/rejeição de nota → não previsto no MVP
- Aprovação por terceiros (auditoria multi-médico) → pós-MVP
- Integração com PEPs → Epic 5

**Estimativa de Complexidade:** `M` (Medium) — backend puro, bem delimitado; RLS e idempotência são os pontos críticos

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: API
**Secondary Type(s)**: Database, Security
**Complexity**: Alta — lógica de state machine, RLS crítico, auditoria LGPD, idempotência

### Specialized Agent Assignment
**Primary Agents**:
- @dev: Endpoints PATCH e POST, state machine `updateNotaStatus()`, Realtime notification
- @architect: Validar contrato de API, estratégia de idempotência e RLS

**Supporting Agents**:
- @data-engineer: Migration para adicionar `approved_by`, `approved_at` na tabela `notas`
- @qa: Testes de RLS (tentar aprovar nota de outro médico) e idempotência

### Quality Gate Tasks
- [ ] Pre-Commit (@dev): RLS policy validation — rodar `supabase db test` para garantir que policies bloqueiam acesso cross-médico
- [ ] Pre-PR (@architect): API contract validation — endpoints compatíveis com Story 3.1 (consumer) e Story 3.3 (producer)
- [ ] Pre-PR (@qa): Teste de segurança — médico B tentando aprovar nota do médico A deve retornar 403

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (ex: falta de RLS check, nota aprovada sem criar versão em `notas_versoes`)
- HIGH issues: document_as_debt (ex: race condition em aprovação simultânea)

### CodeRabbit Focus Areas
**Primary Focus**:
- RLS: verificar que TODA query usa client com JWT do médico — nunca `supabaseAdmin` para operações de médico
- State machine: `updateNotaStatus()` deve validar transição antes de UPDATE — bloqueio de transições inválidas (ex: 'approved' → 'draft')

**Secondary Focus**:
- Idempotência: usar `ON CONFLICT DO NOTHING` ou check de status antes de INSERT em `notas_versoes`
- Auditoria LGPD: garantir que `editado_por` e `approved_by` sejam sempre preenchidos

## Tasks / Subtasks

- [x] **Migration Supabase** (AC: 2, 3, 9)
  - [x] Criar `supabase/migrations/007_nota_approval.sql`:
    - [x] Adicionar colunas `approved_by UUID REFERENCES medicos(id)`, `approved_at TIMESTAMPTZ` na tabela `notas`
    - [x] Adicionar coluna `pdf_status TEXT CHECK (pdf_status IN ('none', 'pending', 'processing', 'ready', 'failed')) DEFAULT 'none'` na tabela `notas` (para Story 3.3)
    - [x] Verificar se `notas_versoes` já existe (Story 2.3) — `editado_por` já existe
  - [x] RLS policies existentes cobrem as novas colunas (via policy de UPDATE em notas)

- [x] **State Machine** (AC: 6)
  - [x] Criar `packages/api/src/services/nota.service.ts` com `validateStatusTransition()`
  - [x] Validação de transições permitidas: `draft` → `approved`, `reviewed` → `approved`
  - [x] Transições inválidas lançam `ApiError` com code `INVALID_STATUS_TRANSITION` (400)

- [x] **Endpoint PATCH — Salvar Rascunho** (AC: 1, 9)
  - [x] `PATCH /api/v1/notas/:id` em `packages/api/src/routes/notas.routes.ts`
  - [x] Body: `{ soap_json: SoapJson, cids_sugeridos: CidSugestao[] }` com Zod validation
  - [x] Trigger `notas_versioning` cria snapshot automático em `notas_versoes` ao UPDATE soap_json
  - [x] Não altera `status` — apenas persiste o rascunho editado

- [x] **Endpoint POST — Aprovar Nota** (AC: 2, 3, 4, 5, 10)
  - [x] `POST /api/v1/notas/:id/aprovar` em `packages/api/src/routes/notas.routes.ts`
  - [ ] RLS via auth middleware — pendente Epic 1 (marcado como tech debt, igual Story 3.1)
  - [x] `validateStatusTransition()` para transição `draft → approved`
  - [x] Registrar `approved_by` e `approved_at` na `nota`
  - [x] Criar snapshot final em `notas_versoes` manualmente (trigger só ativa em soap_json UPDATE)
  - [x] Idempotência: se `status === 'approved'`, retornar 200 sem re-processar (AC: 4)

- [x] **Realtime Notification** (AC: 7)
  - [x] Postgres Changes do Supabase Realtime notifica automaticamente ao UPDATE de `status`
  - [x] Clientes inscritos no canal `consulta:{id}` recebem o evento sem broadcast explícito

- [x] **Mobile — Fluxo de Aprovação** (AC: 8)
  - [x] `apps/mobile/services/nota.service.ts` com `salvarRascunho()` e `aprovar()`
  - [x] `apps/mobile/app/consulta/aprovar/[id].tsx` — tela de confirmação + loading + erro
  - [x] `apps/mobile/stores/nota.store.ts` — Zustand store com `updateStatus()`
  - [x] Redirect para `/consulta/pdf/[id]` após aprovação bem-sucedida

- [x] **Shared Types Update** (AC: 2)
  - [x] `approved_by`, `approved_at`, `pdf_status`, `PdfStatus` adicionados em `packages/shared/src/types/nota.ts`

- [x] **Testes** (AC: 1–10)
  - [x] Unitários: `validateStatusTransition()` — 4 casos (draft→approved, reviewed→approved, approved→draft bloqueado, exported→approved bloqueado)
  - [x] Unitários: `saveNotaDraft()` — 2 casos (sucesso, not found)
  - [x] Unitários: `aprovarNota()` — 4 casos (aprovação, idempotência, not found, invalid transition)
  - [ ] Integração + segurança: médico B tentando aprovar nota de médico A → requer Epic 1 auth

## Dev Notes

### Schema Existente (Story 2.3)
```sql
-- Tabela notas (já existe):
CREATE TABLE notas (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  consulta_id UUID REFERENCES consultas(id),
  transcricao_id UUID REFERENCES transcricoes(id),
  soap_json JSONB NOT NULL,
  cids_sugeridos JSONB,
  status TEXT CHECK (status IN ('draft', 'reviewed', 'approved')),
  baixa_confianca BOOLEAN DEFAULT false,
  versao INTEGER DEFAULT 1,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
-- notas_versoes (já existe em 2.3):
CREATE TABLE notas_versoes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  nota_id UUID REFERENCES notas(id),
  soap_json JSONB NOT NULL,
  versao INTEGER,
  editado_por UUID REFERENCES medicos(id),
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```
[Source: docs/stories/2.3.soap-note-generation.md#schema-de-referência]

### Migration 007 (adições desta story)
```sql
-- Adicionar na tabela notas:
ALTER TABLE notas
  ADD COLUMN approved_by UUID REFERENCES medicos(id),
  ADD COLUMN approved_at TIMESTAMPTZ,
  ADD COLUMN pdf_status TEXT CHECK (pdf_status IN ('none', 'pending', 'processing', 'ready', 'failed')) DEFAULT 'none';

-- RLS policy atualizada:
CREATE POLICY "Médico acessa suas notas" ON notas
  FOR ALL USING (
    consulta_id IN (
      SELECT id FROM consultas WHERE medico_id = auth.uid()
    )
  );
```

### ApiError Padronizado
```typescript
// Códigos novos a adicionar em packages/api/src/errors.ts:
'INVALID_STATUS_TRANSITION' // 400 — ex: tentar reprovar nota já aprovada
'NOTA_NOT_FOUND'            // 404 — nota não encontrada ou sem acesso
```
[Source: docs/architecture/padroes-de-codigo.md#códigos-de-erro-padrão]

### Source Tree (arquivos relevantes)
```
packages/api/src/routes/notas.routes.ts      ← adicionar PATCH e POST /aprovar
packages/api/src/services/nota.service.ts    ← implementar updateNotaStatus()
packages/api/src/repositories/nota.repository.ts ← queries de update
supabase/migrations/007_nota_approval.sql    ← criar (novo)
packages/shared/src/types/nota.ts            ← estender com approved_by, approved_at, pdf_status
apps/mobile/services/nota.service.ts         ← adicionar método aprovar()
```
[Source: docs/architecture/arvore-de-origem.md]

### Dependência Crítica
- Story 3.1 DEVE estar concluída antes — o endpoint `PATCH /api/v1/notas/:id` é consumido pelo auto-save do SoapEditor

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-25 | 0.1 | Story criada a partir do Epic 3 (Review & Approval Flow) | River (SM) |
| 2026-05-25 | 0.2 | Validação PO: GO (8/10) — adicionado Scope OUT e estimativa de complexidade (M); status Draft → Ready | Pax (PO) |
| 2026-05-26 | 0.3 | Implementação YOLO: migration 007, nota.service.ts (state machine + aprovação), PATCH + POST /aprovar, mobile service + store + tela aprovar, 10 testes unitários; status Ready → InReview | Dex (Dev) |
